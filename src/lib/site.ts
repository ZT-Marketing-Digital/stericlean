/**
 * TODO(deploy): trocar pelo domínio definitivo antes de publicar.
 * Usado em canonical, og:url e og:image — o WhatsApp e o Facebook exigem
 * URL absoluta na og:image, então este valor precisa estar correto.
 */
export const SITE_URL = "https://stericleanbrasil.com.br";

export const OG_IMAGE = `${SITE_URL}/og-image.jpg`;
